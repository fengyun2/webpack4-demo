import React from 'react'

import styles from './index.scss'

const About = () => (
  <div className={styles.container}>
    <header className={styles.header}>
      <h3 className={styles.title}>关于我页面</h3>
    </header>
  </div>
)

export default About
